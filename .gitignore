node_modules/
.next/
dist/
*.db
*.db-journal
*.db-wal
*.db-shm
# Local encryption key for stored platform credentials — must never be committed.
*.key
# Shared secret the dashboard presents to the orchestrator. Same rule.
*.token
.env
.env.local
scripts/logs/
# Pre-promotion snapshots (DB backup, prior dist/.next) kept locally so a
# failed promotion can roll back without rebuilding. Not portable — machine-
# and moment-specific, never meant to be shared or version-controlled.
promotion-snapshots/
.screenshots/
.screenshots-*/
coverage/
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
